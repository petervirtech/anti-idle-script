[project]
name = "anti-idle-script"
version = "0.1.0"
description = "A Python script to prevent system idling by simulating keyboard and mouse activity."
readme = "README.md"
requires-python = ">=3.13"
dependencies = []
